#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../vega-lite/bin/vl2png" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../vega-lite/bin/vl2png" "$@"
  ret=$?
fi
exit $ret
