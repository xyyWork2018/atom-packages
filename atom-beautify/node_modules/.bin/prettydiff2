#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../prettydiff2/bin/prettydiff" "$@"
  ret=$?
else 
  node  "$basedir/../prettydiff2/bin/prettydiff" "$@"
  ret=$?
fi
exit $ret
